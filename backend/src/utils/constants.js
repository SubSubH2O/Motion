const INVITATION_STATUS = {
    PENDING: "pending",
    ACCEPTED: "accepted",
    REJECTED: "rejected"
}

const GROUP_ROLE = {
    EDITOR: "editor",
    VIEWER: "viewer"
}

const NOTE_HISTORY_ACTION = {
    CREATE: "create",
    EDIT: "edit",
    DELETE: "delete"
}

const CHART_TYPE = {
    BAR: 'bar',
    LINE: "line",
    PIE: "pie",
    BAR: "bar",
    SCATTER: "scatter",
    AREA: "area",
    RADAR: "radar",
    DOUGHNUT: "doughnut",
    POLAR: "polar"
}

const FRIENDSHIP_STATUS = {
    PENDING: "pending",
    ACCEPTED: "accepted",
    REJECTED: "rejected"
}

module.exports = {
    INVITATION_STATUS,
    GROUP_ROLE,
    NOTE_HISTORY_ACTION,
    CHART_TYPE,
    FRIENDSHIP_STATUS
};